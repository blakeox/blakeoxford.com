{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build site and run quality gate",
			"type": "shell",
			"command": "pnpm clean && pnpm install --frozen-lockfile && pnpm build && pnpm deploy:quality-gate",
			"problemMatcher": [
				"$eslint-stylish"
			],
			"group": "build"
		},
		{
			"label": "Build site and run quality gate",
			"type": "shell",
			"command": "pnpm clean && pnpm install --frozen-lockfile && pnpm build && pnpm deploy:quality-gate",
			"group": "build"
		},
		{
			"label": "Build site and run quality gate",
			"type": "shell",
			"command": "pnpm clean && pnpm install --frozen-lockfile && pnpm build && pnpm deploy:quality-gate",
			"group": "build"
		}
	]
}