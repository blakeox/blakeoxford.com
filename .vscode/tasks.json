{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build site and run quality gate",
			"type": "shell",
			"command": "pnpm quality clean && pnpm install --frozen-lockfile && pnpm build && pnpm check",
			"problemMatcher": [
				"$eslint-stylish"
			],
			"group": "build"
		},
		{
			"label": "Build site and run quality gate",
			"type": "shell",
			"command": "pnpm quality clean && pnpm install --frozen-lockfile && pnpm build && pnpm check",
			"group": "build"
		},
		{
			"label": "Build site and run quality gate",
			"type": "shell",
			"command": "pnpm quality clean && pnpm install --frozen-lockfile && pnpm build && pnpm check",
			"group": "build"
		},
		{
			"label": "Build site and run quality gate",
			"type": "shell",
			"command": "pnpm quality clean && pnpm install --frozen-lockfile && pnpm build && pnpm check",
			"group": "build"
		},
		{
			"label": "Build site and run quality gate",
			"type": "shell",
			"command": "pnpm quality clean && pnpm install --frozen-lockfile && pnpm build && pnpm check",
			"group": "build"
		}
	]
}