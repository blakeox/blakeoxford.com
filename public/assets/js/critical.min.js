function trackEvent(eventName,eventData = {}) {if (Array.isArray(window.dataLayer)) {window.dataLayer.push({event:eventName,...eventData });}if (typeof gtag === 'function') {gtag('event',eventName,eventData);}else if (typeof plausible === 'function') {plausible(eventName,{props:eventData });}else if (typeof fathom === 'object' && typeof fathom.trackEvent === 'function') {fathom.trackEvent(eventName,eventData);}if (typeof clarity === 'function') {clarity('track',eventName,eventData);}if ( !Array.isArray(window.dataLayer) && typeof gtag !== 'function' && typeof plausible !== 'function' && (typeof fathom !== 'object' || typeof fathom.trackEvent !== 'function') && typeof clarity !== 'function' ) {console.debug('[Analytics]',eventName,eventData);}}function trackEventLegacy({category,action,label = '',value = undefined }) {if (window && window.dataLayer) {window.dataLayer.push({event:'custom_event',event_category:category,event_action:action,event_label:label,value:value,});}}function trackPageView(path) {if (window && window.dataLayer) {window.dataLayer.push({event:'pageview',page_path:path || window.location.pathname,});}}class ErrorHandlingSystem {constructor() {this.errorQueue = [];this.isShowingError = false;this.init();}init() {this.setupGlobalErrorHandling();this.setupFormErrorHandling();this.setupNetworkErrorHandling();this.setupUserFeedbackSystem();this.createErrorDisplay();}setupGlobalErrorHandling() {window.addEventListener('error',(event) => {this.handleError({type:'javascript',message:'An unexpected error occurred',details:event.error?.message,technical:event.error?.stack,severity:'error' });});window.addEventListener('unhandledrejection',(event) => {this.handleError({type:'promise',message:'A network or loading error occurred',details:event.reason,severity:'warning' });});document.addEventListener('error',(event) => {if (event.target !== window) {this.handleResourceError(event.target);}},true);}setupFormErrorHandling() {document.addEventListener('invalid',(event) => {this.handleFieldValidation(event.target);},true);document.addEventListener('submit',(event) => {const form = event.target;if (form.tagName === 'FORM') {this.validateForm(form);}});}setupNetworkErrorHandling() {const originalFetch = window.fetch;window.fetch = async (...args) => {try {const response = await originalFetch(...args);if (!response.ok) {this.handleNetworkError(response);}return response;}catch (error) {this.handleError({type:'network',message:'Connection failed. Please check your internet connection.',details:error.message,severity:'error',actions:[ {label:'Retry',action:() => window.fetch(...args) },{label:'Refresh Page',action:() => window.location.reload() }] });throw error;}};}setupUserFeedbackSystem() {this.setupSuccessFeedback();this.setupLoadingStates();this.setupConfirmationDialogs();}handleError(errorInfo) {console.error('Error handled:',errorInfo);this.errorQueue.push({...errorInfo,timestamp:new Date().toISOString(),id:Math.random().toString(36).substr(2,9) });if (!this.isShowingError) {this.showNextError();}this.announceError(errorInfo);}handleResourceError(element) {const tagName = element.tagName.toLowerCase();const src = element.src || element.href;let message = 'Failed to load resource';let userMessage = 'Some content failed to load';let shouldShowError = true;switch (tagName) {case 'img':message = `Failed to load image:${src}`;userMessage = 'An image failed to load';this.handleImageError(element);shouldShowError = false;console.warn('Image failed to load:',src);break;case 'script':message = `Failed to load script:${src}`;userMessage = 'A feature may not work properly due to a loading error';break;case 'link':message = `Failed to load stylesheet:${src}`;userMessage = 'Some styling may not display correctly';break;}if (shouldShowError) {this.handleError({type:'resource',message:userMessage,details:message,severity:'warning' });}}handleImageError(img) {const currentSrc = img.src;const fallbackImage = '/assets/images/blake-logo-fallback.png';if (!currentSrc.includes('blake-logo-fallback.png') && !img.hasAttribute('data-fallback-tried')) {img.setAttribute('data-fallback-tried','true');img.src = fallbackImage;return;}img.style.display = 'none';const altText = img.alt || 'Project image';const placeholder = document.createElement('div');placeholder.className = 'image-error-placeholder bg-white dark:bg-gray-800 rounded-lg flex flex-col items-center justify-center text-gray-600 dark:text-gray-400 text-sm border-2 border-gray-200 dark:border-gray-600';placeholder.style.width = img.style.width || '100%';placeholder.style.height = img.style.height || '200px';placeholder.style.minHeight = '150px';const logoIcon = document.createElement('div');logoIcon.className = 'text-4xl font-bold text-blue-600 mb-2';logoIcon.textContent = 'B';const text = document.createElement('div');text.textContent = 'Blake Oxford Portfolio';placeholder.appendChild(logoIcon);placeholder.appendChild(text);placeholder.setAttribute('role','img');placeholder.setAttribute('aria-label',altText);img.parentNode.insertBefore(placeholder,img);}handleNetworkError(response) {let message = 'Network error occurred';const url = response.url || 'unknown resource';switch (response.status) {case 404:if (url.includes('/assets/images/')) {message = `Image not found:${url.split('/').pop()}`;}else if (url.includes('/api/')) {message = 'API endpoint not found';}else {message = 'The requested content was not found';}if (!url.includes('/assets/images/')) {console.warn('404 Error:',url);}break;case 500:message = 'Server error occurred. Please try again later.';break;case 503:message = 'Service temporarily unavailable';break;default:message = `Network error (${response.status}):${response.statusText}`;}if (response.status === 404 && url.includes('/assets/images/')) {console.warn('Image not found:',url);return;}this.handleError({type:'network',message,severity:'error',status:response.status });}handleFieldValidation(field) {const fieldName = this.getFieldLabel(field);let message = field.validationMessage;if (field.validity.valueMissing) {message = `${fieldName}is required`;}else if (field.validity.typeMismatch) {if (field.type === 'email') {message = 'Please enter a valid email address';}else if (field.type === 'url') {message = 'Please enter a valid URL';}}else if (field.validity.tooShort) {message = `${fieldName}must be at least ${field.minLength}characters`;}else if (field.validity.tooLong) {message = `${fieldName}must be no more than ${field.maxLength}characters`;}else if (field.validity.patternMismatch) {message = `${fieldName}format is invalid`;}this.showFieldError(field,message);}validateForm(form) {const errors = [];const fields = form.querySelectorAll('input,textarea,select');fields.forEach(field => {if (!field.validity.valid) {errors.push({field,message:this.getFieldErrorMessage(field) });}});if (errors.length > 0) {this.showFormErrors(form,errors);return false;}return true;}showFieldError(field,message) {this.clearFieldError(field);const errorId = `${field.id || field.name}-error`;const errorElement = document.createElement('div');errorElement.id = errorId;errorElement.className = 'field-error';errorElement.setAttribute('role','alert');errorElement.setAttribute('aria-live','polite');errorElement.textContent = message;field.parentNode.insertBefore(errorElement,field.nextSibling);field.setAttribute('aria-describedby',errorId);field.setAttribute('aria-invalid','true');field.classList.add('error');field.focus();}clearFieldError(field) {const errorId = `${field.id || field.name}-error`;const errorElement = document.getElementById(errorId);if (errorElement) {errorElement.remove();}field.removeAttribute('aria-describedby');field.removeAttribute('aria-invalid');field.classList.remove('error');}showFormErrors(form,errors) {let errorSummary = form.querySelector('.form-error-summary');if (!errorSummary) {errorSummary = document.createElement('div');errorSummary.className = 'form-error-summary';errorSummary.setAttribute('role','alert');errorSummary.setAttribute('aria-live','assertive');form.insertBefore(errorSummary,form.firstChild);}const errorList = document.createElement('ul');errorList.className = 'error-list';errors.forEach(({field,message }) => {const listItem = document.createElement('li');const link = document.createElement('a');link.href = `#${field.id}`;link.textContent = message;link.addEventListener('click',(e) => {e.preventDefault();field.focus();});listItem.appendChild(link);errorList.appendChild(listItem);});errorSummary.innerHTML = ' <h3>Please correct the following errors:</h3> ';errorSummary.appendChild(errorList);errorSummary.focus();}createErrorDisplay() {const errorDisplay = document.createElement('div');errorDisplay.id = 'error-display';errorDisplay.className = 'error-display';errorDisplay.setAttribute('role','alert');errorDisplay.setAttribute('aria-live','assertive');errorDisplay.innerHTML = ' <div class="error-content"> <div class="error-icon">⚠️</div> <div class="error-message"> <h4 class="error-title"></h4> <p class="error-description"></p> </div> <div class="error-actions"> <button class="error-close" aria-label="Close error message">×</button> </div> </div> ';document.body.appendChild(errorDisplay);errorDisplay.querySelector('.error-close').addEventListener('click',() => {this.hideError();});errorDisplay.addEventListener('click',() => {this.hideError();});}showNextError() {if (this.errorQueue.length === 0 || this.isShowingError) return;const error = this.errorQueue.shift();const errorDisplay = document.getElementById('error-display');errorDisplay.querySelector('.error-title').textContent = error.message;errorDisplay.querySelector('.error-description').textContent = error.details || '';errorDisplay.className = `error-display error-${error.severity}`;errorDisplay.classList.add('show');this.isShowingError = true;const timeout = error.severity === 'error' ? 10000 :5000;setTimeout(() => {if (this.isShowingError) {this.hideError();}},timeout);}hideError() {const errorDisplay = document.getElementById('error-display');errorDisplay.classList.remove('show');this.isShowingError = false;setTimeout(() => {if (this.errorQueue.length > 0) {this.showNextError();}},300);}announceError(errorInfo) {const liveRegion = document.getElementById('live-region');if (liveRegion) {const priority = errorInfo.severity === 'error' ? 'assertive' :'polite';liveRegion.setAttribute('aria-live',priority);liveRegion.textContent = errorInfo.message;setTimeout(() => {liveRegion.textContent = '';},1000);}}setupSuccessFeedback() {document.addEventListener('form-success',(event) => {this.showSuccess(event.detail.message);});}setupLoadingStates() {document.addEventListener('loading-start',(event) => {this.showLoading(event.detail.message || 'Loading...');});document.addEventListener('loading-end',() => {this.hideLoading();});}setupConfirmationDialogs() {document.addEventListener('click',(event) => {if (event.target.hasAttribute('data-confirm')) {const message = event.target.getAttribute('data-confirm');if (!confirm(message)) {event.preventDefault();}}});}showSuccess(message) {const notification = this.createNotification(message,'success');this.showNotification(notification);}showLoading(message) {const loader = document.createElement('div');loader.id = 'loading-indicator';loader.className = 'loading-indicator';loader.setAttribute('role','status');loader.setAttribute('aria-live','polite');loader.innerHTML = ` <div class="loading-spinner"></div> <span class="loading-message">${message}</span> `;document.body.appendChild(loader);}hideLoading() {const loader = document.getElementById('loading-indicator');if (loader) {loader.remove();}}createNotification(message,type) {const notification = document.createElement('div');notification.className = `notification notification-${type}`;notification.setAttribute('role','alert');notification.innerHTML = ` <div class="notification-content"> <span class="notification-message">${message}</span> <button class="notification-close" aria-label="Close notification">×</button> </div> `;notification.querySelector('.notification-close').addEventListener('click',() => {notification.remove();});return notification;}showNotification(notification) {document.body.appendChild(notification);setTimeout(() => {if (notification.parentNode) {notification.remove();}},5000);}getFieldLabel(field) {const label = document.querySelector(`label[for="${field.id}"]`);if (label) {return label.textContent.trim();}const ariaLabel = field.getAttribute('aria-label');if (ariaLabel) {return ariaLabel;}return field.name || field.placeholder || 'Field';}getFieldErrorMessage(field) {const fieldName = this.getFieldLabel(field);if (field.validity.valueMissing) {return `${fieldName}is required`;}else if (field.validity.typeMismatch) {if (field.type === 'email') {return 'Please enter a valid email address';}}else if (field.validity.tooShort) {return `${fieldName}must be at least ${field.minLength}characters`;}else if (field.validity.tooLong) {return `${fieldName}must be no more than ${field.maxLength}characters`;}return field.validationMessage || `${fieldName}is invalid`;}}if (typeof document !== 'undefined') {document.addEventListener('DOMContentLoaded',() => {new ErrorHandlingSystem();});}class ProgressiveEnhancement {constructor() {this.features = new Map();this.init();}init() {document.documentElement.classList.add('js-enabled');document.documentElement.classList.remove('no-js');this.setupCoreEnhancements();this.setupAccessibilityEnhancements();this.setupPerformanceEnhancements();}setupCoreEnhancements() {this.enhanceForms();this.enhanceNavigation();this.enhanceContent();}enhanceForms() {const forms = document.querySelectorAll('form');forms.forEach(form => {form.classList.add('js-enhanced');const submitButtons = form.querySelectorAll('button[type="submit"]');submitButtons.forEach(button => {button.addEventListener('click',() => {button.setAttribute('aria-busy','true');button.classList.add('loading');});});form.addEventListener('invalid',(e) => {e.preventDefault();this.showFieldError(e.target);},true);});}enhanceNavigation() {const mobileToggle = document.querySelector('#nav-toggle');const mobileMenu = document.querySelector('#nav-mobile-links');if (mobileToggle && mobileMenu) {mobileMenu.classList.add('js-enhanced');mobileToggle.setAttribute('aria-expanded','false');mobileToggle.setAttribute('aria-controls','nav-mobile-links');}}enhanceContent() {const images = document.querySelectorAll('img[data-src]');if ('IntersectionObserver' in window) {this.setupLazyLoading(images);}else {images.forEach(img => {img.src = img.dataset.src;img.classList.add('loaded');});}}setupAccessibilityEnhancements() {this.setupEnhancedKeyboardNav();this.setupScreenReaderEnhancements();this.setupMotionPreferences();}setupEnhancedKeyboardNav() {const skipLinks = document.querySelectorAll('.skip-link');skipLinks.forEach(link => {link.addEventListener('focus',() => {link.style.transform = 'translateY(0)';});link.addEventListener('blur',() => {link.style.transform = 'translateY(-100%)';});});const navigableLists = document.querySelectorAll('[role="list"],ul,ol');navigableLists.forEach(list => {this.addArrowKeyNavigation(list);});}setupScreenReaderEnhancements() {if (!document.querySelector('#live-region')) {const liveRegion = document.createElement('div');liveRegion.id = 'live-region';liveRegion.setAttribute('aria-live','polite');liveRegion.setAttribute('aria-atomic','true');liveRegion.className = 'sr-only';document.body.appendChild(liveRegion);}const forms = document.querySelectorAll('form');forms.forEach(form => {form.addEventListener('submit',() => {this.announce('Form submitted. Please wait for confirmation.');});});}setupMotionPreferences() {const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion:reduce)');const handleMotionPreference = (e) => {if (e.matches) {document.documentElement.classList.add('reduce-motion');this.announce('Animations reduced for accessibility.');}else {document.documentElement.classList.remove('reduce-motion');}};prefersReducedMotion.addEventListener('change',handleMotionPreference);handleMotionPreference(prefersReducedMotion);}setupPerformanceEnhancements() {if ('connection' in navigator) {const connection = navigator.connection;if (connection.effectiveType === 'slow-2g' || connection.effectiveType === '2g') {document.documentElement.classList.add('slow-connection');this.disableHeavyAnimations();}}if ('getBattery' in navigator) {navigator.getBattery().then(battery => {if (battery.level < 0.2) {document.documentElement.classList.add('low-battery');this.enablePowerSavingMode();}});}}setupLazyLoading(images) {const imageObserver = new IntersectionObserver((entries) => {entries.forEach(entry => {if (entry.isIntersecting) {const img = entry.target;img.src = img.dataset.src;img.classList.add('loaded');imageObserver.unobserve(img);}});});images.forEach(img => imageObserver.observe(img));}addArrowKeyNavigation(list) {const items = list.querySelectorAll('a,button,[tabindex]:not([tabindex="-1"])');list.addEventListener('keydown',(e) => {const currentIndex = Array.from(items).indexOf(e.target);let newIndex;switch (e.key) {case 'ArrowDown':e.preventDefault();newIndex = currentIndex < items.length - 1 ? currentIndex + 1 :0;break;case 'ArrowUp':e.preventDefault();newIndex = currentIndex > 0 ? currentIndex - 1 :items.length - 1;break;case 'Home':e.preventDefault();newIndex = 0;break;case 'End':e.preventDefault();newIndex = items.length - 1;break;default:return;}items[newIndex]?.focus();});}showFieldError(field) {const errorId = `${field.id}-error`;let errorElement = document.getElementById(errorId);if (!errorElement) {errorElement = document.createElement('div');errorElement.id = errorId;errorElement.className = 'field-error';errorElement.setAttribute('role','alert');field.parentNode.appendChild(errorElement);}errorElement.textContent = field.validationMessage;field.setAttribute('aria-describedby',errorId);field.setAttribute('aria-invalid','true');}announce(message,priority = 'polite') {const liveRegion = document.getElementById('live-region');if (liveRegion) {liveRegion.setAttribute('aria-live',priority);liveRegion.textContent = message;setTimeout(() => {liveRegion.textContent = '';},1000);}}disableHeavyAnimations() {const style = document.createElement('style');style.textContent = ' * {animation-duration:0.01ms !important;animation-iteration-count:1 !important;transition-duration:0.01ms !important;}';document.head.appendChild(style);}enablePowerSavingMode() {document.documentElement.classList.add('power-saving');this.disableHeavyAnimations();const heavyElements = document.querySelectorAll('video,.parallax,.heavy-animation');heavyElements.forEach(el => {el.style.display = 'none';});}}if (typeof document !== 'undefined') {if (document.readyState === 'loading') {document.addEventListener('DOMContentLoaded',() => {new ProgressiveEnhancement();});}else {new ProgressiveEnhancement();}}