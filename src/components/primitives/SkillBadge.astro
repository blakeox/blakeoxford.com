---
import { cn } from '../../utils/cn';

interface Props {
 skill: string;
 class?: string;
}

const { skill, class: className = '' } = Astro.props;
---

<li class={cn('px-3 py-2 rounded-lg bg-accent-subtle text-foreground/90', className)}>
 {skill}
</li>
