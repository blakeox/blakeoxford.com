---
interface Props {
 skill: string;
 class?: string;
}

const { skill, class: className = '' } = Astro.props;
---

<li class={`px-3 py-2 rounded-lg bg-accent/10 text-foreground/90 ${className}`}>
 {skill}
</li>
