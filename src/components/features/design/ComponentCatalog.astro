---
import Badge from '@/components/primitives/Badge.astro';
import BaseCard from '@/components/primitives/BaseCard.astro';
import Button from '@/components/primitives/Button.astro';
import { componentDocs, getCategories, getAllTags, type ComponentDoc } from '@/data/componentDocs';
import { componentVisualBaselines } from '@/data/componentVisualBaselines';
import { getButtonClasses } from '@/lib/design-system/recipes';
import { headingSizeClass } from '@/lib/typeScale';

const categories = getCategories();
const componentsByCategory = categories.map((category) => ({
  category,
  components: componentDocs.filter((doc) => doc.category === category),
}));
const allTags = getAllTags();
const tagFilterIdle = getButtonClasses({
  variant: 'outline',
  size: 'sm',
  className: 'tag-filter rounded-full',
});
const tagFilterActive = getButtonClasses({
  variant: 'primary',
  size: 'sm',
  className: 'tag-filter rounded-full',
});
---

<section
  class="mt-16"
  aria-labelledby="component-catalog"
  data-tag-idle={tagFilterIdle}
  data-tag-active={tagFilterActive}
>
  <header class="mb-8">
    <h2
      id="component-catalog"
      class:list={['font-heading font-semibold text-foreground', headingSizeClass('title')]}
    >
      Searchable Catalog
    </h2>
    <p class="mt-2 text-muted-foreground">
      {componentDocs.length} documented components across {categories.length} categories.
    </p>
  </header>

  <div class="mb-12">
    <label for="component-search" class="sr-only">
      Search components
    </label>
    <input
      type="search"
      id="component-search"
      placeholder="Search components by name, description, or tag..."
      class="w-full rounded-xl border border-border bg-field-bg px-6 py-4 text-lg text-foreground transition focus-visible:border-accent focus-visible:ring-2 focus-visible:ring-accent/25 focus-visible:outline-none"
    />
    <p
      id="component-results"
      class="mt-3 text-sm text-muted-foreground"
      role="status"
      aria-live="polite"
    >
      Showing {componentDocs.length} components.
    </p>
    <p id="component-empty" class="mt-3 hidden text-sm text-warning-emphasis" role="status">
      No components match those filters. Try a different term or choose All.
    </p>
  </div>

  <div class="mb-12">
    <h3 class="mb-4 text-xxs font-semibold tracking-label text-subtle-foreground uppercase">
      Filter by Tag
    </h3>
    <div class="flex flex-wrap gap-2">
      <button class={tagFilterIdle} data-tag="all" aria-pressed="false" type="button">
        All
      </button>
      {allTags.map((tag) => (
        <button class={tagFilterIdle} data-tag={tag} aria-pressed="false" type="button">
          {tag}
        </button>
      ))}
    </div>
  </div>

  {componentsByCategory.map(({ category, components }) => (
    <section class="mb-16">
      <h3
        class:list={[
          'mb-8 font-heading font-semibold text-foreground',
          headingSizeClass('section'),
        ]}
      >
        {category} Components
        <span class="ml-2 text-base font-normal text-subtle-foreground">({components.length})</span>
      </h3>

      <div class="space-y-8">
        {components.map((component: ComponentDoc) => (
          <BaseCard
            as="article"
            variant="subtle"
            hover="none"
            padding="none"
            class="component-card min-w-0 overflow-hidden transition-colors hover:border-accent/40"
            data-component-name={component.name.toLowerCase()}
            data-component-tags={component.tags?.join(',') || ''}
          >
            <div class="p-5 sm:p-8">
              <header class="mb-6">
                <div class="flex items-start justify-between gap-4">
                  <div>
                    <h4
                      class:list={[
                        'font-heading font-semibold text-foreground',
                        headingSizeClass('title'),
                      ]}
                    >
                      {component.name}
                    </h4>
                    {component.subcategory && (
                      <p class="mt-1 text-sm text-subtle-foreground">
                        {component.category} › {component.subcategory}
                      </p>
                    )}
                  </div>
                  <Button
                    variant="outline"
                    size="sm"
                    href={`https://github.com/blakeox/blakeoxford.com/blob/main/${component.filePath}`}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="shrink-0"
                  >
                    View Source
                  </Button>
                </div>

                <p class="mt-4 text-muted-foreground">{component.description}</p>

                {component.visualTier && (
                  <p class="mt-3 text-sm">
                    <span class="font-semibold text-foreground">Visual tier:</span>{' '}
                    <Badge variant="subtle" size="xs">
                      {component.visualTier}
                    </Badge>
                  </p>
                )}

                {component.tokenDependencies && component.tokenDependencies.length > 0 && (
                  <div class="mt-3">
                    <p class="text-sm font-semibold text-foreground">Token dependencies</p>
                    <div class="mt-2 flex flex-wrap gap-2">
                      {component.tokenDependencies.map((token) => (
                        <code class="rounded bg-accent-subtle px-2 py-1 text-xs text-accent-emphasis">
                          {token}
                        </code>
                      ))}
                    </div>
                  </div>
                )}

                {component.visualBaseline && (
                  <p class="mt-3 text-sm text-muted-foreground">
                    <span class="font-semibold text-foreground">Visual baseline:</span> Playwright
                    snapshot{' '}
                    <code class="text-accent-emphasis">
                      {componentVisualBaselines[component.visualBaseline].snapshotFile}
                    </code>
                    on <code>{componentVisualBaselines[component.visualBaseline].route}</code>
                  </p>
                )}

                {component.tags && component.tags.length > 0 && (
                  <div class="mt-4 flex flex-wrap gap-2">
                    {component.tags.map((tag) => (
                      <Badge variant="subtle" size="xs">
                        {tag}
                      </Badge>
                    ))}
                  </div>
                )}
              </header>

              {component.props && component.props.length > 0 && (
                <div class="mb-6">
                  <h5 class="mb-3 text-lg font-semibold text-foreground">Props</h5>
                  <div class="max-w-full overflow-x-auto" tabindex="0">
                    <table class="w-full text-sm">
                      <thead class="border-b border-border">
                        <tr class="text-left">
                          <th class="pr-4 pb-2 font-semibold">Name</th>
                          <th class="pr-4 pb-2 font-semibold">Type</th>
                          <th class="pr-4 pb-2 font-semibold">Default</th>
                          <th class="pb-2 font-semibold">Description</th>
                        </tr>
                      </thead>
                      <tbody class="divide-y divide-border">
                        {component.props.map((prop) => (
                          <tr>
                            <td class="py-3 pr-4">
                              <code class="rounded bg-accent-subtle px-2 py-1 text-accent-emphasis">
                                {prop.name}
                                {prop.required && <span class="text-error">*</span>}
                              </code>
                            </td>
                            <td class="py-3 pr-4">
                              <code class="text-muted-foreground">{prop.type}</code>
                            </td>
                            <td class="py-3 pr-4">
                              {prop.default ? (
                                <code class="text-muted-foreground">{prop.default}</code>
                              ) : (
                                <span class="text-subtle-foreground/80">—</span>
                              )}
                            </td>
                            <td class="py-3 text-muted-foreground">{prop.description}</td>
                          </tr>
                        ))}
                      </tbody>
                    </table>
                  </div>
                </div>
              )}

              {component.examples && component.examples.length > 0 && (
                <div class="mb-6">
                  <h5 class="mb-3 text-lg font-semibold text-foreground">Examples</h5>
                  <div class="space-y-4">
                    {component.examples.map((example) => (
                      <div>
                        {example.title && (
                          <p class="mb-2 text-sm font-medium text-muted-foreground">
                            {example.title}
                          </p>
                        )}
                        {example.description && (
                          <p class="mb-2 text-sm text-subtle-foreground">{example.description}</p>
                        )}
                        <pre
                          class="max-w-full overflow-x-auto rounded-xl bg-code-surface p-4 text-sm text-code-foreground"
                          tabindex="0"
                        >
                            <code>{example.code}</code>
                          </pre>
                      </div>
                    ))}
                  </div>
                </div>
              )}

              {component.accessibility && component.accessibility.length > 0 && (
                <div class="mb-6">
                  <h5 class="mb-3 text-lg font-semibold text-foreground">Accessibility</h5>
                  <ul class="space-y-2">
                    {component.accessibility.map((feature) => (
                      <li class="flex items-start gap-2 text-sm text-muted-foreground">
                        <span class="mt-1 text-success">✓</span>
                        <span>{feature}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              )}

              {component.performance && component.performance.length > 0 && (
                <div>
                  <h5 class="mb-3 text-lg font-semibold text-foreground">Performance</h5>
                  <ul class="space-y-2">
                    {component.performance.map((feature) => (
                      <li class="flex items-start gap-2 text-sm text-muted-foreground">
                        <span class="mt-1 text-info">⚡</span>
                        <span>{feature}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              )}
            </div>
          </BaseCard>
        ))}
      </div>
    </section>
  ))}

  <BaseCard as="footer" variant="subtle" hover="none" padding="none" class="mt-16 text-center">
    <div class="p-5 sm:p-8">
      <h3
        class:list={['mb-4 font-heading font-semibold text-foreground', headingSizeClass('title')]}
      >
        Documentation Statistics
      </h3>
      <div class="grid gap-6 sm:grid-cols-3">
        <div>
          <p class="text-4xl font-bold text-accent-emphasis">{componentDocs.length}</p>
          <p class="mt-1 text-sm text-subtle-foreground">Components Documented</p>
        </div>
        <div>
          <p class="text-4xl font-bold text-accent-emphasis">{categories.length}</p>
          <p class="mt-1 text-sm text-subtle-foreground">Categories</p>
        </div>
        <div>
          <p class="text-4xl font-bold text-accent-emphasis">{allTags.length}</p>
          <p class="mt-1 text-sm text-subtle-foreground">Unique Tags</p>
        </div>
      </div>
    </div>
  </BaseCard>
</section>

<script>
  const catalogRoot = document.querySelector(
    '[aria-labelledby="component-catalog"]'
  ) as HTMLElement;
  const tagIdle = catalogRoot?.dataset.tagIdle || '';
  const tagActive = catalogRoot?.dataset.tagActive || '';
  const searchInput = document.getElementById('component-search') as HTMLInputElement;
  const componentCards = document.querySelectorAll('.component-card');
  const tagFilters = document.querySelectorAll('.tag-filter');
  const resultStatus = document.getElementById('component-results');
  const emptyState = document.getElementById('component-empty');

  let currentTag = 'all';

  function setTagPressed(button: Element) {
    tagFilters.forEach((btn) => {
      const isActive = btn === button;
      btn.setAttribute('aria-pressed', String(isActive));
      (btn as HTMLElement).className = isActive ? tagActive : tagIdle;
    });
  }

  function filterComponents() {
    const searchTerm = searchInput?.value.toLowerCase() || '';

    let visibleCount = 0;
    componentCards.forEach((card) => {
      const tags = card.getAttribute('data-component-tags') || '';
      const tagList = tags ? tags.split(',') : [];
      const text = card.textContent?.toLowerCase() || '';
      const matchesSearch = text.includes(searchTerm);
      const matchesTag = currentTag === 'all' || tagList.includes(currentTag);

      if (matchesSearch && matchesTag) {
        (card as HTMLElement).style.display = 'block';
        visibleCount += 1;
      } else {
        (card as HTMLElement).style.display = 'none';
      }
    });
    if (resultStatus) {
      resultStatus.textContent = `Showing ${visibleCount} of ${componentCards.length} components.`;
    }
    emptyState?.classList.toggle('hidden', visibleCount > 0);
  }

  searchInput?.addEventListener('input', filterComponents);

  tagFilters.forEach((button) => {
    button.addEventListener('click', () => {
      setTagPressed(button);
      currentTag = button.getAttribute('data-tag') || 'all';
      filterComponents();
    });
  });

  const allButton = document.querySelector('[data-tag="all"]');
  if (allButton) setTagPressed(allButton);
</script>

<style>
  pre code {
    font-family: 'Monaco', 'Courier New', monospace;
    line-height: 1.6;
  }

  .component-card {
    transition:
      opacity 0.2s ease,
      border-color 0.2s ease;
  }

  @media (max-width: 768px) {
    table {
      font-size: 0.875rem;
    }
  }
</style>
