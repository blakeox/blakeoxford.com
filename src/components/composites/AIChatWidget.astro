---
/**
 * AIChatWidget — Ask overlay (shared shell with Find) + FAB launcher.
 */
import AIChatIsland from '@/features/chat/AIChatIsland';
import ChatLauncherIsland from '@/features/chat/ChatLauncherIsland';
---

<div data-ai-chat-widget>
  <div class="ai-chat-wrapper" data-ai-chat-open="false">
    <ChatLauncherIsland client:only="react" />
    <AIChatIsland client:only="react" />
  </div>
</div>
